name: VM4 Strict CI
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  vm4-strict-ci:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'controlled strict-status success'
