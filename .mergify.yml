merge_queue:
  mode: serial

queue_rules:
  - name: VM4 fast-forward required-review parity
    merge_method: fast-forward
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = VM4_FF_REVIEW_HOLD_NEVER
